#include<stdio.h>
#include<unistd.h>
int main() {
    int child,parent;
    child=getpid();
    parent=getpid();
    printf("Child process id: %d\n",child);
    printf("Parent process id: %d",parent);
    return 0;
}
